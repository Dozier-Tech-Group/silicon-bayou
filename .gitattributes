# The provenance file is an on-chain hash commitment: bytes must never be normalized.
generator/merged-public/provenance.json -text
# MP sealed metadata is pinned to IPFS by exact bytes (CIDs live onchain): no CRLF rewrites.
metadata/mp/*.json -text
# Season manifests are keccak256 hash commitments echoed on-chain: bytes must never be normalized.
game/**/*.json -text
